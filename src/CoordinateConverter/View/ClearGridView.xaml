<UserControl x:Class="CoordinateConverter.View.ClearGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
        Height="96.429"
        Width="320.595"
        >
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10"
                   Text="Таблица содержит данные. Очистить таблицу?" FontSize="14"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="10,5">
            <CheckBox  VerticalAlignment="Center" Margin="0" IsChecked="{Binding Path=ClearCheck}" />
            <TextBlock  Margin="10,0,0,0" VerticalAlignment="Center" Text="Запомнить выбор очистки таблицы" FontSize="14"/>
        </StackPanel>
    </Grid>
</UserControl>
